78-Comando para generar imagen con docker y spring

./mvnw spring-boot:build-image "-Dspring-boot.build-image.imageName=4xe1/i-ecom"

------

79-comandos

docker pull <image>
docker push <username/image>
docker run -it -d -p <host-port>:<container-port> --name <name> <image>
docker start <container id/name>
docker stop <container id/name>
docker rm <container id/name>
docker rmi <image>
docker ps 
docker ps -a
docker images
docker exec -it <cont-name/cont-id> bash
docker build -t <username/image> .
docker logs <cont-name/cont-id>
docker inspect <cont-name/cont-id> 

------

80-run docker container

docker run -p 8080:8080 4xe1/i-ecom  

------

84-docker network

docker network create indi-net

docker run -p 5532:5432 --name postgres14 --network indi-net -e POSTGRES_PASSWORD=sasa -e POSTGRES_DB=db-ecom-indi -d postgres:14-alpine

el prof usó esta:

docker run --name postgres-indi --network indi-net -e POSTGRES_PASSWORD=sasa -e POSTGRES_DB=db-ecom-indi -d postgres:14-alpine

conexion entre 2 contenedores de manera interactiva:

docker exec -it contenedor1 ping contenedor2

------

85-comandos profe para desplegar contenedores de postgreSQL y pgAdmin

docker run -d --name postgres_container -e POSTGRES_USER=XXX -e POSTGRES_PASSWORD=XXX -e PGDATA=/data/postgres -v postgres:/data/postgres -p 5432:5432 --network YYY --restart unless-stopped postgres:14

docker run -d --name pgadmin_container -e PGADMIN_DEFAULT_EMAIL=pgadmin4pgadmin.org -e PGADMIN_DEFAULT_PASSWORD=admin -e PGADMIN_CONFIG_SERVER_MODE=False -v pgadmin:/var/lib/pgadmin -p 5050:80 --network YYY --restart unless-stopped dpage/pgadmin4


docker-compose en el proyecto ...  

docker compose up

docker compose up -d

docker compose down

------

87- POSTGRE Y PG ADMIN CON DOCKER 

ir al localhost:5050/browser/  .... con los contenedores de postgres running

logueate con los datos del docker compose

registra un servidor:

general: 
 - name: postgres.
connection:
 - 


::::::::::::::::::::::::::::::::::::::::::::::::


MICROSERVICIOS

:::::::::::::::::::::::::::::::::


MONGO 

net start mongoDB

net stop mongoDB

EJECUTA MONGOSH EN POWER SHELL

mongosh

muestra las bases de datos

show dbs

-------

Mongo nube 

usa correo de burrocarnicero

burrocarnicero_db_user

NSVRkBOQPv0vPHwI


------


Nube de postgres 

Neon DB

npx neonctl@latest init

psql 'postgresql://neondb_owner:npg_YgyCfxXaAS48@ep-empty-bush-adpf6ten-pooler.c-2.us-east-1.aws.neon.tech/db-product-indi?sslmode=require&channel_binding=require'



-----------------------------------------

152 - MQ Rabbit

# latest RabbitMQ 4.x
docker run -it --rm --name rabbitmq -p 5672:5672 -p 15672:15672 rabbitmq:4-management


-----------------------------------------

303 - KEYCLOAK

docker run -p 127.0.0.1:8443:8080 -e KC_BOOTSTRAP_ADMIN_USERNAME=admin -e KC_BOOTSTRAP_ADMIN_PASSWORD=admin quay.io/keycloak/keycloak:26.7.3 start-dev

docker run -d -p 127.0.0.1:8443:8080 -e KC_BOOTSTRAP_ADMIN_USERNAME=admin -e KC_BOOTSTRAP_ADMIN_PASSWORD=admin quay.io/keycloak/keycloak:26.7.3 start-dev

localhost:8443
admin
admin

admin_1
sasa











 
