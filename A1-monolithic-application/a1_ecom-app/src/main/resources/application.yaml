#spring.application.name=ecom-app
#spring.h2.console.enabled=true
#spring.datasource.url=jdbc:h2:mem:test
#spring.jpa.show-sql=true
#spring.jpa.hibernate.ddl-auto=create
#management.endpoints.web.exposure.include=*

spring:
  application:
    name: ecom-app
  datasource:
    url: jdbc:postgresql://localhost:5432/db-ecom-indi
    username: postgres
    password: sasa
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  #h2:
  #  console:
  #    enabled: true
  #datasource:
  #  url: jdbc:h2:mem:test
  #jpa:
  #  show-sql: true
  #  hibernate:
  #    ddl-auto: create

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
    shutdown:
      enabled: true
  info:
    env:
      enabled: true

server:
  shutdown: graceful

info:
  app:
    name: MyNewApp
    description: App aksjhda aksjhd asdkjh aksjhd
    version: 1.0.0
