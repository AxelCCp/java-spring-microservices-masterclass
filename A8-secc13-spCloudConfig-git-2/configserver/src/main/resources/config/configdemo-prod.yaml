app:
  deployment:
    id: 101
    version: 1.2.3
    name: "Production-Build NATIVE"
    type: "Production Build NATIVE"

spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: "*"